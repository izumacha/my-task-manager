"""アプリ全体のデザイントークン（配色・フォント・余白）を集約するモジュール。

TimeTree を参考にした「ポップで親しみやすい」見た目を、ここで定義した
トークンだけで再現できるようにしている。色やフォントを変えたいときは
このファイルだけを編集すればよく、Web 版・アプリ版・スマホ版へ展開する
際も同じトークン表を共有することで**プラットフォーム間の見た目の差異**を
最小化することを狙う（CLAUDE.md「クロスプラットフォーム設計」を参照）。

GUI フレームワーク（tkinter）に依存しない純粋な定数・関数のみを置く。
"""
from __future__ import annotations

# ------------------------------------------------------------ ブランド / 基調色

# TimeTree 風のさわやかなティールグリーンをブランドカラーにする。
BRAND = "#2BC4A8"          # プライマリ（主要ボタン・現在進行中の強調）
BRAND_DARK = "#1FA890"     # プライマリの押下/ホバー相当
BRAND_SOFT = "#E5F8F4"     # ブランド色の淡いトーン（バッジ背景など）

# ページ背景は淡いグレー、カードは白にして「浮いた」印象を出す。
BG = "#F4F6F8"             # ウィンドウ全体の背景
CARD = "#FFFFFF"           # カード（パネル）の背景
BORDER = "#E3E7EC"         # カードの境界線・区切り線

# テキスト色（コントラストを確保しつつ柔らかく）
TEXT = "#2B3038"           # 主要テキスト
TEXT_MUTED = "#8A94A6"     # 補助テキスト・空き時間など
TEXT_ON_BRAND = "#FFFFFF"  # ブランド色背景の上に載せる文字

# 状態色
DONE_BG = "#EEF1F4"        # 完了タスクの背景
DONE_FG = "#A7B0BE"        # 完了タスクの文字
PAST_BG = "#FDECEC"        # 期限超過（未完了）の背景
PAST_FG = "#E0524B"        # 期限超過の文字
# 現在進行中の文字色は _block_colors() が theme.BRAND_DARK を直接参照するため、
# ここに専用トークンは置かない（カレンダーが Canvas カード描画へ刷新された際に
# 参照元が切り替わり、以前ここにあった NOW_FG は未使用のデッドコードになっていた。
# CLAUDE.md §6「デッドコードを残さない」に従い削除する）。
SUGGEST_FG = BRAND_DARK    # 「あとでやる」の空き時間提案（ブランドの暗色を参照する）

# ------------------------------------------------------------ タスクのカラーパレット

# TimeTree のように 1 つ 1 つの予定を色分けするためのポップなパレット。
# （背景色, その上に載せる文字色）の組で持つ。
CATEGORY_COLORS: tuple[tuple[str, str], ...] = (  # カテゴリ色パレット（背景色・文字色のペアを格納するタプル）
    ("#FFE2E0", "#C0392B"),  # コーラル
    ("#FFF1D6", "#B9770E"),  # イエロー
    ("#E3F1FF", "#2266A8"),  # スカイブルー
    ("#E8E6FF", "#5B4BC4"),  # パープル
    ("#E3F8E8", "#1E9E54"),  # グリーン
    ("#FFE6F1", "#C2417E"),  # ピンク
    ("#FFEAD9", "#C46314"),  # オレンジ
    ("#DCF5F2", "#0E8C7C"),  # ティール
)

# 各タスク色の先頭に付ける「丸ポチ」相当の絵文字（凡例的な彩り）。
CATEGORY_DOTS: tuple[str, ...] = ("🔴", "🟡", "🔵", "🟣", "🟢", "🩷", "🟠", "🩵")  # カテゴリを視覚的に区別するための絵文字ドットのタプル

# ------------------------------------------------------------ フォント

# tkinter のフォント指定で使うファミリ。OS 既定の "system" を基本にする。
FONT_FAMILY = "system"  # OS の標準フォントファミリ名を定数として定義する
FONT_BASE = (FONT_FAMILY, 11)  # 標準サイズのフォント設定（ファミリ・サイズのタプル）
FONT_SMALL = (FONT_FAMILY, 10)  # やや小さいフォント設定（補足テキストなどに使用する）
FONT_HEADING = (FONT_FAMILY, 13, "bold")  # 見出し用の大きめ太字フォント設定
FONT_DATE = (FONT_FAMILY, 16, "bold")  # 日付表示用のさらに大きい太字フォント設定
FONT_STATS = (FONT_FAMILY, 11, "bold")  # 統計値表示用の太字フォント設定
FONT_BOLD = (FONT_FAMILY, 11, "bold")  # 強調テキスト用の太字フォント設定

# Treeview の行の高さ（ゆったり見せて可読性を上げる）。
ROW_HEIGHT = 30  # Treeview の 1 行あたりの高さ（ピクセル単位）

# ------------------------------------------------------------ レイアウト（ウィンドウ寸法・余白・間隔）

# ウィンドウやパネルの寸法トークン。app.py に生の数値を書かず、必ずここを参照する
# （CLAUDE.md §6「定数・ラベルは一元管理する」。値を変えるときはこのファイルだけ編集する）。
WINDOW_MIN_WIDTH = 900     # ウィンドウを縮小できる最小幅（ピクセル単位）
WINDOW_MIN_HEIGHT = 540    # ウィンドウを縮小できる最小高さ（ピクセル単位）
TIMELINE_PANEL_WIDTH = 460  # カレンダー Canvas の初期描画幅（ピクセル単位。実幅は <Configure> で更新される）

# 「あとでやる」リスト（Treeview）の列幅（ピクセル単位）。
BACKLOG_COL_TITLE_WIDTH = 170  # タスク名列の幅（ピクセル単位）
BACKLOG_COL_DUR_WIDTH = 70     # 所要時間列の幅（ピクセル単位）
BACKLOG_COL_RECUR_WIDTH = 80   # 繰り返し列の幅（ピクセル単位）

# コンテナ（フレーム/カード）の内側余白 padding。大きさ順に LG > MD > SM。
PAD_LG = 18  # アプリ全体を包む外側フレームの内側余白（ピクセル単位）
PAD_MD = 14  # カード（カレンダー/あとでやるパネル）の内側余白（ピクセル単位）
PAD_SM = 12  # 入力フォームカードの内側余白（ピクセル単位）

# ウィジェット同士の間隔（padx / pady）。大きさ順のスケールで管理する。
SPACE_XS = 4    # ラベルとすぐ隣の入力部品の間など、最小の間隔（ピクセル単位）
SPACE_SM = 6    # アイコンとテキストの間などの小さい間隔（ピクセル単位）
SPACE_MD = 8    # ボタン同士・ステータスバー上などの中間隔（ピクセル単位）
SPACE_LG = 10   # 見出しの下・タイトル入力欄の右などのやや大きい間隔（ピクセル単位）
SPACE_XL = 12   # 入力グループ間・アクションボタン行の上などの大きい間隔（ピクセル単位）
SPACE_XXL = 14  # ヘッダ/入力カードなどセクション（行）間の縦の間隔（ピクセル単位）

# スケールに乗らない用途特化の間隔トークン。
HEADER_GROUP_GAP = 16  # ヘッダ内で起床/就寝グループを日付から離す左側の間隔（ピクセル単位）
PANEL_GAP = 9          # カレンダーとあとでやるパネルの間の横の隙間（ピクセル単位）
ENTRY_IPADY = 3        # タスク名入力欄の上下の内側余白 ipady（ピクセル単位）

# ------------------------------------------------------------ カレンダー（デイビュー）

# 縦軸カレンダーの 1 時間あたりの高さ（px）と、左の時刻ラベル幅・上下の余白。
# 所要時間に比例した高さのブロックでタスクを描くため、分→px の換算に使う。
HOUR_HEIGHT = 60           # 1 時間あたりの高さ（ピクセル単位）：60px = 1 時間
CAL_GUTTER = 56            # 左の時刻ラベル列の幅（ピクセル単位）
CAL_PAD_TOP = 12           # 描画の上下余白（ピクセル単位）
CAL_BLOCK_GAP = 4          # ブロック左右/相互の隙間（ピクセル単位）
# レーン（横列）が狭いとき、左右の固定隙間 CAL_BLOCK_GAP を引くとブロック幅が
# 負になり、タスクが見えず・押せなくなる。これを防ぐため隙間は「レーン幅 ×
# この割合」までに抑える（同時刻に多数のタスクが重なる最悪条件でも幅を正に保つ）。
CAL_BLOCK_GAP_MAX_RATIO = 0.25  # 隙間がレーン幅に占めてよい最大割合（片側ぶん）
GRID_LINE = "#E9EDF2"      # 正時の罫線（時境界を示す線の色）
GRID_LINE_HALF = "#F2F5F8"  # 30 分の罫線（さらに淡く）
GRID_LABEL = "#9AA0A6"     # 時刻ラベルの文字（カレンダー左端に表示する時刻テキストの色）
NOW_LINE = "#FF6F61"       # 現在時刻ライン（コーラル）

# Any Planner 風のタスクカード要素。
CAL_STRIPE_W = 5           # カード左端のカテゴリ色ストライプの幅（ピクセル単位）
CAL_CHECK_R = 7            # チェックボックス円の半径（ピクセル単位）
CAL_RADIUS = 10            # カードの角丸半径（ピクセル単位）

# 所要時間が極端に短いタスクでも押せる大きさを保つための、ブロックの最低高さ（px）。
# レーン割り当て（_assign_lanes）もこの最低高さぶんの見た目の占有を考慮しないと、
# 隣接する短時間タスク同士が同じレーンで描画上重なってしまうため、両者で共有する。
CAL_MIN_BLOCK_HEIGHT = 24  # ブロックの最低高さ（ピクセル単位）

# ブロックにタイトルと時刻の 2 行を載せられる最小の高さ（px）。
# これ未満の短いタスクは、文字が重ならないようタイトルのみを 1 行で中央表示する。
CAL_MIN_TEXT_HEIGHT = 46  # 2 行テキストを表示できる最小ブロック高さ（ピクセル単位）


def category_index(key: str) -> int:
    """タスクを表す文字列（id 等）から安定したカラー番号を返す。

    同じ key には常に同じ色を割り当てるため、再描画やプラットフォームを
    またいでも配色がぶれない。
    """
    if not key:  # key が空文字列の場合はインデックス 0（最初の色）を返す
        return 0  # 空の key に対してデフォルトのカラー番号 0 を返す
    total = sum(ord(ch) for ch in key)  # key の各文字のコードポイントを合計して数値化する
    return total % len(CATEGORY_COLORS)  # 合計をパレットの色数で割った余りをインデックスとして返す


def category_color(key: str) -> tuple[str, str]:
    """key に対応する (背景色, 文字色) を返す。"""
    return CATEGORY_COLORS[category_index(key)]  # key から算出したインデックスに対応する色ペアを返す


def category_dot(key: str) -> str:
    """key に対応する丸ポチ絵文字を返す。"""
    return CATEGORY_DOTS[category_index(key)]  # key から算出したインデックスに対応する絵文字を返す
